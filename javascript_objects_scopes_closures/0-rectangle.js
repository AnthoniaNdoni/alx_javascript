class Rectangle {
    // The class is empty, with no attributes or methods defined.
  }
  
  // You can create instances of the Rectangle class later when needed.
  const myRectangle = new Rectangle();